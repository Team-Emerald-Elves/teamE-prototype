$kind: http-request
name: Create Service Request
method: POST
url: '{{baseUrl}}/create-srvreq'
order: 3000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{authToken}}'
body:
  type: json
  content: |-
    {
      "description": "Sample service request description",
      "uname": "johndoe"
    }
